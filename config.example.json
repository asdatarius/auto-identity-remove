{
  "_comment": "Copy this file to config.json and fill in your details. Run `node setup.js` to be guided interactively.",

  "person": {
    "firstName": "Jane",
    "lastName": "Doe",
    "fullName": "Jane Doe",
    "aliases": ["Jan Doe", "Jane M Doe"],
    "city": "Austin",
    "country": "US",
    "_country_note": "2-letter country code. US (default), CA, GB, AU, etc. Non-US users: 'state' holds province/region; 'zip' holds postal code (any format, e.g. K1A 0A6 or SW1A 1AA); 'phoneFormatted' is the raw phone number with no US formatting.",
    "state": "TX",
    "_state_note": "For non-US: province or region (e.g. ON for Ontario, NSW for New South Wales)",
    "zip": "73301",
    "_zip_note": "For non-US: postal code in your country's format (e.g. K1A 0A6, SW1A 1AA, 2000)",
    "email": "jane.doe@example.com",
    "phone": "5125550000",
    "phoneFormatted": "(512) 555-0000",
    "_phone_note": "For non-US: phoneFormatted equals phone with no reformatting applied"
  },

  "capsolver": {
    "_comment": "Sign up free at capsolver.com — pay-as-you-go, costs pennies/month for this use case",
    "apiKey": "CAP-YOUR_KEY_HERE"
  },

  "accounts": {
    "_comment": "For sites that require a login. Leave password blank — setup.js will prompt you to create the account once, then store credentials here.",
    "spokeo":       { "email": "", "password": "" },
    "beenverified": { "email": "", "password": "" },
    "mylife":       { "email": "", "password": "" }
  },

  "notify": {
    "_comment": "iMessage number to text results to (macOS only). Leave blank to disable.",
    "textTo": "+15125550000",
    "_webhook_comment": "Optional webhook URL for notifications on any OS (ntfy.sh, Slack, Discord). Leave blank to disable.",
    "webhook": ""
  },

  "email": {
    "_comment": "Optional SMTP config for sending opt-out emails on Linux/Windows (or as an alternative to Mail.app on macOS). Leave the smtp block absent or empty to use Mail.app on macOS.",
    "smtp": {
      "host": "smtp.example.com",
      "port": 587,
      "user": "you@example.com",
      "pass": "YOUR_APP_PASSWORD",
      "from": "you@example.com"
    }
  },

  "profileDir": "~/.config/auto-identity-remove"
}
